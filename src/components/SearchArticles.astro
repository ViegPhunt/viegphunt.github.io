---
import SearchBox from './SearchBox.astro';
---

<div class="my-[var(--section-stack)]">
	<div class="mb-4">
		<div class="text-2xl font-bold text-text">Search Articles</div>
	</div>
	<form class="w-full" id="searchForm">
		<SearchBox 
			id="search"
			placeholder="Search by title, description or tags..."
			onSearch="search-query-changed"
		/>
	</form>
</div>

<script>
	function initSearchForm() {
		const searchForm = document.getElementById("searchForm");
		if (!searchForm) return;

		searchForm.addEventListener("submit", (e) => {
			e.preventDefault();
		});
	}

	initSearchForm();
	document.addEventListener("astro:page-load", initSearchForm);
</script>